<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abhinav.com.languageapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lang"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LangHomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LangLogin" />
        <activity android:name=".LangSelectAct" />
        <activity android:name=".LangRandomeGenerator"></activity>
    </application>

</manifest>